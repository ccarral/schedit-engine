use crate::grid::UaemGrid;
use schedule_engine::engine::{engine_main, EngineParams};
use wasm_bindgen::prelude::*;


#[wasm_bindgen]
pub fn api_init_grids() -> Result<JsValue, JsValue> {
    unimplemented!();
}

#[cfg(test)]
mod test {
    use super::*;
    fn test_main() {
        unimplemented!();
    }
}
